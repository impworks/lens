<Window x:Class="GraphicScript.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="600" Width="800" MinWidth="640" MinHeight="480">
    <Grid SnapsToDevicePixels="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Margin="10" BorderBrush="Black" BorderThickness="1">
            <TextBox Margin="0" Background="#171E1F" Foreground="White" FontFamily="Consolas" BorderThickness="0" Text="{Binding Code, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
        </Border>
        
        <GridSplitter Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" Width="3" ShowsPreview="True" ResizeBehavior="PreviousAndNext" />
        <GridSplitter Grid.Row="1" Grid.ColumnSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="3" ShowsPreview="True" ResizeBehavior="PreviousAndNext" />
        
        <Border Grid.Column="2" BorderBrush="Black" BorderThickness="1" Margin="0,10,10,10" />
        
        <TextBlock Grid.Row="2" Grid.ColumnSpan="3" Text="{Binding Status}" />
    </Grid>
</Window>
